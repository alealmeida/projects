include ../assets/_includes/mixins.pug
include ../assets/_includes/var.pug

block vars
doctype html
html
	head
		block head
			title DRINKSTORE
			meta(charset="utf-8")
			+link("style.css")
			//[if IE] <link href="/css/ie.css" media="screen, projection" rel="stylesheet" type="text/css" /> [endif]
			
			+script("jquery.js")
			+script("drinkstore.js")
		style(type="text/css").
			html,body{margin:0;padding:0;}
	body
		.d-store
			header(id="hero")
				+header
				a.logo(href="http://busca.paodeacucar.com/ppc/drinkstore")
					h1 Drinkstore
			nav.navbar
				ul
					-for(i=1;i<=5;i++)
						+menu(i)
			block content
				-for(n=1;n<=3;n++)
					+estrutura(n)
			footer#footer
				.container
					p Sujeito a disponibilidade de estoque. SE BEBER, NÃO DIRIJA.